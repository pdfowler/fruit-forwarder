{
  "config": {
    "step": {
      "reconfigure": {
        "title": "Change allowed reminder lists and calendars",
        "description": "Enter exact EventKit IDs, one per line. Match these with the Mac configuration. Removed lists lose their queued edits. Pairing and existing entity identities are preserved.",
        "data": {
          "allowed_list_ids": "Allowed reminder list IDs",
          "allowed_calendar_ids": "Allowed calendar IDs"
        }
      },
      "user": {
        "title": "Connect Fruit Forwarder",
        "description": "Run `icloud-reminders-bridge pair` on the macOS bridge, then paste the copied token and the exact list IDs from `icloud-reminders-bridge discover`.",
        "data": {
          "name": "Name",
          "bridge_id": "Bridge identifier",
          "pairing_token": "Pairing token",
          "allowed_list_ids": "Allowed EventKit reminder list IDs (one per line)",
          "allowed_calendar_ids": "Allowed EventKit calendar IDs (optional, one per line)"
        }
      }
    },
    "error": {
      "invalid_token": "The pairing token is not a valid 256-bit bridge token.",
      "no_lists": "Enter at least one exact EventKit reminder list or calendar ID."
    },
    "abort": {
      "reconfigure_successful": "The allowed lists and calendars were updated.",
      "already_configured": "This bridge is already configured."
    }
  }
}
